<h1>Editing Concert</h1>

<%= render 'form', concert: @concert %>
<%= link_to "Back-don't save", concerts_path, :class => 'btn btn-warning btn-sm active' %>

<% if false %>
<%= link_to 'Show', @concert %> |
<% end %>